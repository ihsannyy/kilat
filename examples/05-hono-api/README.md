# Hono API

REST API with Hono framework on Kilat.

## Run

```bash
kilat run src/index.js
```

## Endpoints

- `GET /` - Hello message
- `GET /api/time` - Current time
- `GET /api/user/:id` - User by ID

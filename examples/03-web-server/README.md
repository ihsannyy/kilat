# Web Server

Simple REST API with Kilat.serve.

## Run

```bash
kilat run server.js
```

## Endpoints

- `GET /` - Homepage
- `GET /api/time` - Server time
- `GET /api/echo?msg=hello` - Echo query params
- `GET /api/stats` - Server stats
